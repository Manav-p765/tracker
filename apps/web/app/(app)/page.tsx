import { TodayDashboard } from "./TodayDashboard";

/**
 * Home. The bento dashboard owns its own data, so this is just the entry point.
 */
export default function TodayPage() {
  return <TodayDashboard />;
}
