import { RouteStub } from "@/components/ui/RouteStub";

export default function VaultPage() {
  return (
    <RouteStub
      tag="VAULT"
      title="Vault"
      builtBy="Prompt 3.2"
      note="Paste a caption or transcript; keep the useful part as written text you will actually reread."
    />
  );
}
