import { RouteStub } from "@/components/ui/RouteStub";

export default function EventsPage() {
  return (
    <RouteStub
      tag="EVENTS"
      title="Important events"
      builtBy="Prompt 3.3"
      note="Dated and recurring, with a countdown in days and a reminder ahead of time."
    />
  );
}
