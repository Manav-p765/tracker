{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    // No Node globals here on purpose: this package must stay usable in the
    // browser bundle as well as in apps/api and apps/worker.
    "types": [],
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"]
}
